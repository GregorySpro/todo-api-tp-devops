services:
  api:
    image: node:18-alpine
    command: sh -c "while true; do sleep 3600; done"
    networks:
      - frontend
      - backend
    ports:
      - "3001:3000"

  database:
    image: postgres:15-alpine
    environment:
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: network_demo
    networks:
      - backend

  nginx:
    image: nginx:alpine
    networks:
      - frontend
    ports:
      - "8080:80"

networks:
  frontend:
  backend:
